#include <iostream>
#include "registers.h"

using namespace std;

Registers::Registers (int i_regNum, int i_value)
{
    regNum = i_regNum;
    value = i_value;
}

void Registers::setRegister(int regNum, int value){
while(cin.bad() || regNum > 31);
cin.clear();

}
